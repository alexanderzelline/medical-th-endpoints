meta {
  name: Editar plantilla (parcial)
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/api/v1/notificaciones/plantillas/1/
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
  Authorization: Token {{token}}
}

body:json {
  {
    "asunto": "Nuevo asunto para {{nombres}}"
  }
}
